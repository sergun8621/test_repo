git config --global user.name "Sergey"
git config --global user.email myEmail@example.com
git config --global init.defaultBranch main - меняем имя основной ветки на main вместо master = best practices
